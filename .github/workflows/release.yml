name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Get version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Package skill
        run: |
          # Create distribution directory
          mkdir -p dist

          # Create skill archive (README.md excluded per skill-creator guidelines)
          zip -r "dist/submodule-sync-${{ steps.version.outputs.VERSION }}.skill" \
            SKILL.md \
            scripts/ \
            -x "*.DS_Store" \
            -x "*/.git/*"

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: dist/*.skill
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
